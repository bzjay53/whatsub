# Netlify 배포 설정
[build]
  publish = "public"
  command = ""  # 빌드 프로세스를 건너뜁니다

# 리다이렉트 및 헤더 설정
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    Content-Security-Policy = "frame-ancestors 'none'"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=(), interest-cohort=()"

# CORS 설정: Chrome 확장 프로그램에서 API 요청을 허용
[[headers]]
  for = "/api/*"
  [headers.values]
    Access-Control-Allow-Origin = "chrome-extension://*"
    Access-Control-Allow-Methods = "GET, POST, PUT, DELETE, OPTIONS"
    Access-Control-Allow-Headers = "Origin, X-Requested-With, Content-Type, Accept, Authorization"
    Access-Control-Allow-Credentials = "true"

# 필요한 리다이렉트 설정
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  # 존재하지 않는 파일에 대한 요청만 리다이렉트
  force = false

# 환경 변수 설정
[context.production.environment]
  NETLIFY_ENV = "production"
  NODE_VERSION = "18.x"

[context.deploy-preview.environment]
  NETLIFY_ENV = "deploy-preview"
  NODE_VERSION = "18.x"

# 환경 변수 (민감한 정보는 Netlify 대시보드에서 설정)
[build.environment]
  NETLIFY_NEXT_PLUGIN_SKIP = "false"
  NODE_VERSION = "18"
  CI = "false"
  NEXT_TELEMETRY_DISABLED = "1"
  NPM_FLAGS = "--no-audit --no-fund"
  NEXT_SKIP_TS_CHECK = "true"
  NETLIFY_USE_YARN = "false"

[[plugins]]
  package = "@netlify/plugin-nextjs"
  [plugins.inputs]
    includeFiles = ["public/**/*"]

# 최후의 수단: 정적 HTML로 대체
[build.processing]
  skip_processing = false

# 백업 플랜 - 정적 HTML
# [build]
#   command = "mkdir -p public && echo '<!DOCTYPE html><html><head><meta http-equiv=\"refresh\" content=\"0;url=https://chrome.google.com/webstore/detail/whatsub/YOUR_EXTENSION_ID\"></head><body><p>리디렉션 중...</p></body></html>' > public/index.html"
#   publish = "public" 